import React from "react"
import DrumIcon from "../images/drum-icon.png"

const Header = () => {
    return (
        <div className="footer-section">
            < img className="header-icons" src={DrumIcon} alt="instrument" />
        </div>
    )
}

export default Header

